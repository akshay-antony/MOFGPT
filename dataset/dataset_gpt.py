import torch
from torch.utils.data import Dataset
import numpy as np
import os


class MOFGPTDataset(Dataset):
    def __init__(self,
                 config,
                 tokenizer,
                 mode='train'):
    pass

    